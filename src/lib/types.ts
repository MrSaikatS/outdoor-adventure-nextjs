export type EventType = {
  eId: string;
  eImage: string;
  eTitle: string;
  eDescription: string;
};

export type ServiceType = {
  sId: string;
  sImage: string;
  sTitle: string;
  sDescription: string;
};
